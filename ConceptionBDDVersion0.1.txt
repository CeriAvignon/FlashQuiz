CREATE DATABASE FlashQuiz; //PERMISSION DENIED

CREATE TABLE User(ID_User SERIAL NOT NULL UNIQUE, First_Name VARCHAR(32), Last_Name VARCHAR(32), Login VARCHAR(32) NOT NULL UNIQUE, Password VARCHAR(32), Rank INT);

CREATE TABLE Session_Metadata(ID_Session SERIAL NOT NULL UNIQUE, Opening_Date DATE, Closing_Date DATE, Type_Session BOOL, Title VARCHAR(48), Password VARCHAR(32));

CREATE TABLE Session_Content(ID_Session INT, ID_List INT);

CREATE TABLE List_Metadata(ID_List SERIAL NOT NULL UNIQUE, ID_User INT, Title VARCHAR(32));

CREATE TABLE List_Content(ID_List INT, ID_Question INT);

CREATE TABLE Question(Question_ID SERIAL NOT NULL UNIQUE, Type_Question INT, Content_Question TEXT, ID_Media INT);

CREATE TABLE Scores(ID_Session INT NOT NULL UNIQUE, ID_User INT, Score INT);

CREATE TABLE Answer(ID_Answer SERIAL NOT NULL UNIQUE, ID_Question INT, Content TEXT, Correct BOOL);

CREATE TABLE Media(ID_Media SERIAL NOT NULL UNIQUE, Type INT, Content TEXT);

$co = new PDO("pgsql:host=localhost;dbname=etd","uapv1504061","2TD1MN");
